version: 2.1
orbs:
  cypress: cypress-io/cypress@5.0.1
  node: circleci/node@5.0.0

workflows:
  test-and-deploy:
    jobs:
      - cypress/run:
          name: test
          install-command: npm install
          post-install: npm run db:migrate && npm run db:seed
          start-command: "npm run dev" # Or your specific command to start the app
          context:
            - zephyr_development

      - build-and-deploy:
          requires:
            - test
          context:
            - zephyr_production
          filters:
            branches:
              only: main

jobs:
  build-and-deploy:
    executor: node/default
    steps:
      - checkout
      - node/install-packages
      - run:
          name: Build production
          command: npm run build
      - run:
          name: Deploy to Cloudflare
          command: npm run deploy
